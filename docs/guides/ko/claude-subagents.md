# Claude Code Subagents 워크플로우 (spec-quick 특화)

> 📖 **English guide:** [Claude Code Subagents Workflow](../claude-subagents.md)

이 가이드는 `npx k-sdd@latest --claude-agent`(또는 `--claude-code-agent`)로 제공되는 **Claude Code Subagents** 중, 독자적인 제어 로직을 가진 `spec-quick` 명령어에 초점을 맞춰 설명한다. そ다른 `/kiro:*` 명령어들도 동일한 Subagent를 재사용하지만, 동작은 표준 버전과 동일하므로 여기서는 설명을 생략한다.

## 설치 확인

- `npx k-sdd@latest --claude-agent --lang <언어코드>` 실행
- 생성되는 파일은 다음과 같다
  - `.claude/commands/kiro/`: Spec/Steering/Validation 관련 커맨드 (12개)
  - `.claude/agents/kiro/`: 상세 분석용 Subagent 정의 파일 (9개)
  - `CLAUDE.md`: 퀵 스타트 가이드

## spec-quick을 통한 Subagent 연계 플로우

`spec-quick` 은 다음 4단계를 자동으로 연속 실행하는 매크로 커맨드다.
- `spec-init`(인라인 구현)
- `spec-requirements`
- `spec-design`
- `spec-tasks` 
이 기능의 구현은 `tools/k-sdd/templates/agents/claude-code-agent/commands/spec-quick.md` 에 정의되어 있다.

### 모드

- **인터랙티브 모드 (기본값)**: 각 단계 완료 후, 다음 단계로 진행할지 확인한다. 최초 실행이나 복잡한 기능 개발에 적합하다.
- **자동 모드 (`--auto`)**: TodoWrite로 진행 상황(4/4)을 추적하며, 확인 없이 끝까지 실행한다. 리스크가 낮은 기능의 초안 생성에 적합하다.

두 모드 모두 다음 검증 커맨드는 스킵된다.
- `/kiro:validate-gap` 
- `/kiro:validate-design` 
완료 메시지에서 수동 실행을 권장하므로,
기존 프로젝트(Brownfield)에 기능을 추가하는 경우 반드시 별도로 실행해야 한다.

### 각 단계의 동작

| 단계 | 호출되는 Subagent | 주요 처리 내용 |
| --- | --- | --- |
| 1. 초기화 | 인라인 (Subagent 없음) | `.kiro/specs/{feature}/` 디렉터리를 생성하고, 템플릿 기반으로 `spec.json`과 `requirements.md`골격을 만든다. TodoWrite 첫 항목을 완료 상태로 변경한다. |
| 2. Requirements | `agents/spec-requirements.md` | `/kiro:spec-requirements {feature}`실행. 사용자와의 Q&A를 통해 요구사항 초안을 작성한다. 자동 모드에서는 “다음 단계” 안내를 무시하고 바로 3단계로 이동한다. |
| 3. Design | `agents/spec-design.md` | `/kiro:spec-design {feature} -y`실행한다. 필요 시 `research.md`, `design.md`업데이트한다. TodoWrite 진행률 3/4이다. |
| 4. Tasks | `agents/spec-tasks.md` | `/kiro:spec-tasks {feature} -y` 실행하고, `tasks.md` 출력한다. 요구사항 커버리지 및 병렬 실행 가능 여부를 `(P)` 라벨로 표시한다. 완료 시 TodoWrite 4/4이다. |

자동 모드에서는 Subagent의 가이드 메시지와 무관하게 다음 단계로 자동 진행된다.
반면 인터랙티브 모드에서는 단계 사이마다
“요구사항 정의로 이동할까요?”, “설계로 이동할까요?” 같은 확인이 들어간다.

### 출력물 및 스킵되는 게이트

출력 파일:
- `spec.json`
- `requirements.md`
- `design.md`(필요 시 `research.md` 업데이트)
- `tasks.md`(`(P)` 병렬 실행 라벨 포함)

스킵되는 검증:
- `/kiro:validate-gap`
- `/kiro:validate-design`
- `/kiro:validate-impl`

### Subagent 수동 실행

특정 단계만 다시 실행하고 싶다면, Claude Code 채팅에서 `@agents-spec-design`, `@agents-spec-tasks` 처럼 멘션하면 된다.
이때 설치 시 생성된 `.claude/agents/kiro/*.md`내부의 프롬프트가 사용된다.

## 권장 사용 시나리오

1. `npx k-sdd@latest --claude-agent --lang <code>`실행하여 Subagent를 워크스페이스에 배포한다.
2. `/kiro:steering`(또는 필요 시 `/kiro:steering-custom`) 실행 후, 프로젝트 메모리(Project Memory) 최신화한다.
3. `spec-quick <feature> [--auto]`로 스펙 초안 생성 후, `requirements.md`, `design.md`, `tasks.md` 검토한다.
4. 기존 시스템에 영향이 있을 경우, `/kiro:validate-gap`, `/kiro:validate-design` 반드시 추가 실행한다.
5. 스펙 승인 후, `/kiro:spec-impl`, `/kiro:spec-status`로 구현 및 진행 관리한다.

## Subagent 커스터마이징

1. **템플릿 및 룰 업데이트**: `{{KIRO_DIR}}/settings/templates/*.md`, `{{KIRO_DIR}}/settings/rules/*.md`에 공통 체크리스트 등을 작성하면 모든 에이전트가 참조한다.
2. **Subagent 프롬프트 수정**: `.claude/agents/kiro/*.md`파일을 수정해 우선순위, 리스크 분류, 테스트 전략 등 휴리스틱을 추가한다.
3. **커맨드 기반 실행 조건 제어**: `.claude/commands/kiro/*.md`내 `call_subagent`섹션을 수정해 호출 타이밍 제어한다.
4. **프롬프트 간결화**: Task Tool 출력 영역은 제한적이므로, 긴 지시는 템플릿/룰로 분리하고
Subagent 프롬프트는 핵심만 유지하는 것이 안정적한다.

## 트러블슈팅

| 증상 | 원인 | 해결 방법 |
| --- | --- | --- |
| Subagent가 실행되지 않음 | `--claude-agent`옵션 없이 설치했거나 `.claude/agents/kiro/`가 없음 | `npx k-sdd@latest --claude-agent` 로 재설치함 |
| 분석 범위가 과도함 | 파일 검색 패턴이 너무 넓음 (예: `*.*`) | Subagent 프롬프트에서 검색 패턴을 구체화 |
| 출력이 템플릿과 다름 | 오래된 템플릿 참조 | `{{KIRO_DIR}}/settings/templates` 최신화 후 참조 여부 확인 |

## 관련 링크

- [Spec-Driven Development ワークフロー](spec-driven.md)
- [Docs README](../README.md)
